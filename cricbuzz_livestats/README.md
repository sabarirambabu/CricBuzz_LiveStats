# 🏏 CricBuzz LiveStats

A Streamlit dashboard that shows live cricket matches, player statistics, SQL analytics, and basic CRUD operations using the Cricbuzz API and MySQL.

---

## 🔐 Environment Variables (`.env`)

All sensitive credentials are loaded from a `.env` file (not hard-coded).

Create a `.env` file in the project root:

```env
CRICBUZZ_API_KEY=your_rapidapi_key
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAME=CB

---

### To Run the App

1.Clone the repository
2.Install dependencies, reqiurements.txt attached
3.Run the app in the terminal using "streamlit run app.py"

